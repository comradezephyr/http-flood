Barebone minimal http flooder sritten in python with graceful error handling, load distribution among proxies, configurable threads and total requests.
Run `python httpflood.py --help` for usage or simple `python httpflood.py`
